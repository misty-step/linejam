/**
 * Theme Presets
 *
 * Re-exports from registry for backwards compatibility.
 * New code should import from '../registry' directly.
 */

export {
  themes,
  themeIds,
  defaultThemeId,
  getTheme,
  isValidThemeId,
  kenyaTheme,
  monoTheme,
  vintagePaperTheme,
} from '../registry';
